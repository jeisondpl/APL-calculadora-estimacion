import { ComponentesView } from '@/views/Componentes/ComponentesView'

export const metadata = { title: 'Componentes — APL Calculadora' }

export default function ComponentesPage() {
  return <ComponentesView />
}
